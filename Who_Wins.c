#include <stdio.h>

int main()
{
    int n;
    scanf("%d", &n);
    int tiger, pathan, count = 0;
    for (int i = 1; i < n; i++)
    {
        // scanf("%d %d", &tiger[i], &pathan[i]);
    }
    return 0;
}